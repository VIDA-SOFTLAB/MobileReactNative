import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonsContainer: {
    width: '100%',
    alignItems: 'center',
  },
  coverImage: {
    alignSelf: 'center',
    marginRight: 24,
  },
});
